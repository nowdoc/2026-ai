# Atlassian AI

This repository has a prepared configuration for controlling Atlassian with prompts.

## Config

1. MCP servers at `.cursor/mcp.json`.
2. Setup `.env` file with your Atlassian credentials.

## Usage

1. Enable MCP servers in Cursor.
2. Pick a prompt from `prompts` folder.
